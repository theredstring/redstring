# P2.06a report: `SyncDebugHost`

**Status:** done (orchestrator).

## What changed
- **`src/components/canvas/hosts/SyncDebugHost.jsx`** (memo, `<Profiler id="SyncDebugHost">`) is rendered by CanvasShell after ModalHosts. It holds everything that existed only to feed DebugOverlay:
  - its own `debugConfig` subscription
  - `syncDebugData` and the action ring buffer
  - `buildSyncDebugData` and the 2 s refresh while the overlay is on
  - the retry-engine, force-save, clear-App-cache, direct-probe and dump-auth actions

  The dynamic imports were re-pathed; nothing else changed in the moved code.
- **NodeCanvas** keeps a `debugConfig` subscription, because the carousel takes `debugMode`. NodeCanvas.jsx: 17,822 → **17,313**.
- **Behavior note:** the overlay's "Hide" used to set NodeCanvas's `debugMode` to false, which also turned off the carousel's debug drawing until the next config change. Now it hides only the overlay. Neither version changes the saved setting.
- **Stacking:** DebugOverlay (z 20000) now renders after the canvas overlays and before nothing it could overlap wrongly (LayoutProgress is 1000). It's debug-only.

## Verification
- New F22b: the overlay follows the Settings switch and fills with the sync snapshot.
- `test:ci` PASS. Playwright: 47 passed (F12 expected). `lint:undef` PASS.
