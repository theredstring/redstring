# P4.01 (part a) report: `inputTuning` module; `clampPan`

**Status:** part done (orchestrator). The rest of P4.01 is still open: `clientToCanvas`, the hit-test modules and `edgeHitTest`.

## What changed
- **`src/utils/canvas/input/inputTuning.js`** holds 51 declarations moved **verbatim** from NodeCanvas: platform detection (`isMac`, `isIOS`), the touch, pan-momentum, zoom-momentum and trackpad constants, and `measureTrackpadZoomVelocity`. NodeCanvas imports the 48 it uses.
  - `CONNECTION_DETENT_PX` and `NODE_GROUP_INTERIOR_TINT`, which sat inside that block, stay in NodeCanvas.
  - Reconciling the touch constants (F-44) waits on Grant's answer to Q5; the values are unchanged.
- **`src/utils/canvas/viewportMath.js` `clampPan(pan, zoom, viewportSize, canvasSize)`** replaces the **four byte-identical** inline copies in the camera-framing callbacks. The other copies differ in shape (refs, named max variables, separate axes), so they stay until P4.02's camera controller owns the one write path.
- NodeCanvas.jsx: 15,732 → **15,562**.

## Verification
- New `test/utils/inputTuning.test.js` pins all 45 numeric tuning values and tests the velocity sampler. New `test/utils/viewportMath.test.js`.
- `test:ci` PASS. Playwright: 55 passed, including F2/F3 (pan, zoom), F9 (carousel framing), F15 (graph switch) and F16 (hurtle). `lint:undef` PASS.
