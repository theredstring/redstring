# P1.10 report: the pie and context-menu memos depend only on what they read (plus B-06)

**Status:** done (orchestrator). **Commits:** `8385536` (P1.10), `98a6326` (B-06 + flow F6b).

## What changed
- **Dependency lists** (unused entries found by scanning each body; icons are module constants):

| Memo | Deps before | Deps after |
|---|---|---|
| `nodePieMenuPages` | 48 | 13 |
| `targetPieMenuButtons` | 49 | 19 |
| `edgePieMenuButtons` | 12 | 11 |
| `getCanvasContextMenuOptions` | 11 | 10 |
| `getContextMenuOptions` | 23 | 19 |

  Removed:
  - `panOffset` and `zoomLevel`. Every camera settle rebuilt the pie and re-copied it into state.
  - the lucide icons, `containerRef`, `clipboardRef`
  - values the bodies never read

  After B-06, `wizardEnabled` is in both pie memos; that's the one added to each.
- **The pie's Copy** reads `graphs`, `nodePrototypes` and `edges` from the store at click time. Those three were the only reason the pie depended on the whole-universe maps.
- **B-06:**
  - `wizardEnabled` is now a dependency of both pie memos.
  - Edit and Open in Panel read `rightPanelExpanded` at click time.
- **Not touched** (per the card): the pie → state sync effect and the pie state machine (P5).

## Effect
- **S6** (`--explain`, one run): 30 → 28 commits, 19 → 18 NodeCanvas runs. The `currentPieMenuData` rebuild after the camera settles is gone.
- The two rebuilds on deselect remain: the selection really does change the menu.

## Verification
- **F6b** (new) toggles the wizard while the pie is open on its second page:
  - Without the B-06 fix it fails: Ask The Wizard stays.
  - With the fix it passes.
- A first attempt at a panel-state flow passed with and without the fix, so it wasn't kept.
- Playwright: 35 passed. NodeCanvas tests: 56 passed. `lint:undef` passes.
- NodeCanvas.jsx: 18,412 lines, unchanged.
