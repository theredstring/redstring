# P2.06c report: `UniverseHost` and `UniverseScreens`

**Status:** done (orchestrator).

## What changed
- **`src/components/canvas/hosts/UniverseHost.jsx`** (memo, `<Profiler id="UniverseHost">`) is rendered by CanvasShell before the overlay slot, so the save pill keeps its place right after TypeList. It takes these from NodeCanvas **verbatim** (only the dynamic-import paths and the reconnect-state bindings changed):
  - workspace start-up (`workspaceService.initialize` → READY / SELECT_UNIVERSE / NEEDS_ONBOARDING, auto-connect)
  - the onboarding check that opens StorageSetupModal, and the modal with its four handlers
  - GitHub reconnect: `resolveGitReconnectTarget`, the load-failure effect, `openReconnect`, the missing-App prompt with its auth-event listeners and 1.5 s settle, the save pill's `redstring:open-git-reconnect` event, and GitReconnectModal
  - the Git redirect resume (onboarding, reconnect or panel)
  - the `redstring:open-federation` / `openGitFederation` / `redstring:open-external-link` listeners
  - SaveStatusDisplay
- **Reconnect state** (`universeReconnect`, `universeReconnectTarget`, `universeReconnectDismissed`) is in canvasUIStore, because the canvas error card reads the target and reopens the modal.
- **`src/components/canvas/UniverseScreens.jsx`** holds the loading screen, with its universe-name lookup, and the "not loaded" screen: escape hatch, load error, reconnect card. NodeCanvas renders it in the canvas area where those were, when the universe is loading or not loaded.
- **NodeCanvas** keeps the dead `openOnboardingModal` listener (B-03; D-17 rewires it to the welcome-screen host) and a `showStorageSetupModal` read for the download pill. Eight imports went with the code. NodeCanvas.jsx: 17,215 → **16,464**.
- **Stacking:** both modals are CanvasModals (z 20100+), so their new DOM position changes nothing.

## Verification
- New F24: the loading screen, then a failed load with its error and "Go to Universes" (which opens the Universes tab). F24b: the save pill is on screen. F18 (the Universes-panel event) still passes.
- Shell screenshots: within antialiasing noise of the old layout in all nine states.
- `test:ci` PASS. Playwright: 51 passed (F12 expected). `lint:undef` PASS.
- **Not covered by automated flows:** onboarding on a fresh profile, the GitHub reconnect modal, and the OAuth/App redirect resume. They need a clean profile or real GitHub, and were moved without logic changes. Put them on the end-of-P2 smoke-test list.
