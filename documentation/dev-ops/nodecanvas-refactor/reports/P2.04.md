# P2.04 report: prompts, modals, header searches and text-entry focus in `canvasUIStore`

**Status:** done (orchestrator).

## What changed
- **Into the store, bound in place under their old names:**
  - the six prompts: node name, connection name, abstraction, node-group, swap, new web
  - both header searches
  - the six modal flags: help, settings, merge, storage setup, auto-graph, force sim
  - `isHeaderEditing`
- **F-50 decided: delete the panel focus plumbing (D-19).**
  - The panels' `onFocusChange` was passed from NodeCanvas through Panel to `PanelContentWrapper`, which never called it. So `isLeftPanelInputFocused`/`isRightPanelInputFocused` were never true.
  - Typing in panel fields was already kept from the canvas by the DOM check (`utils/textEntry.js`). F13 covers it.
  - Removed: the two flags (NodeCanvas state, store fields, `useCanvasKeyboard` params), `handleLeftPanelFocusChange`, both `onFocusChange` props, and Panel's/the wrapper's `onFocusChange`.
  - `selectIsTextEntryActive` is now `isHeaderEditing` only. The header title editor is the one field that reports its focus.
- **Tests:**
  - The keyboard hook's "latest flag" test uses `isHeaderEditing`.
  - The store test table dropped the two setters.
- NodeCanvas.jsx: 18,410 → **18,396**; budget lowered.

## Verification
- NodeCanvas, component, store, hook, meta and keyboard tests: 546 passed (plus the updated keyboard test).
- Playwright: 36 passed. F13 (keys typed into a text field never reach the canvas) and F14 (plus sign, name it) included.
- `lint:undef` passes.
