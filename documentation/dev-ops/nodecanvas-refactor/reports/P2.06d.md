# P2.06d report: `SearchHosts`

**Status:** done (orchestrator).

## What changed
- **`src/components/canvas/hosts/SearchHosts.jsx`** (memo, `<Profiler id="SearchHosts">`) is rendered by CanvasShell. It holds the component search, the All Things search and the New Thing prompt, all UnifiedSelectors, which portal to `<body>`, so moving them changes nothing on the page.
  - The component search is its own component, mounted only while open, so the active web's instances (which change on every node move) are followed only then.
  - Picking a Thing flies to its instances through the new **`navigateToPrototypeInstances`** canvas command.
  - The actions call graphStore when they run.
- **NodeCanvas** drops the three selectors and the All Things flag. It keeps `setHeaderSearchVisible` for a keyboard shortcut, and `newWebPrompt` for the keyboard hook's input check. NodeCanvas.jsx: 17,313 → **17,215**.

## Verification
- New F23: New Thing creates and opens a web. F23b: the component search opens the Thing's tab and moves the camera; it fails with the command unregistered.
- `test:ci` PASS. Playwright: 49 passed (F12 expected). `lint:undef` PASS.
