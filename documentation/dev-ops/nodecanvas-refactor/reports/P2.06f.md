# P2.06f report: `ForceSimHost`; layout progress in the store

**Status:** done (orchestrator).

## What changed
- **`src/components/canvas/hosts/ForceSimHost.jsx`** (memo, `<Profiler id="ForceSimHost">`) is rendered by CanvasShell after SearchHosts. It holds ForceSimulationModal and LayoutProgressIndicator.
  - The tuner's settings, font size and graph id come from the stores.
  - Its getters read the live canvas only while it runs, so they are canvas commands: `layoutNodes` (hydrated nodes with measured sizes), `layoutEdges` (with connection names), `draggedNodeIds`, plus `resetConnectionLabelCache` and `cancelAutoLayout`.
  - The command bodies are the old inline getters, unchanged.
- **Layout progress** is `canvasUIStore.layoutProgress`, written by `useGraphLayout` without subscribing. Progress ticks during an auto-layout no longer re-render NodeCanvas; before, each tick was a NodeCanvas render.
- **Stacking:** the tuner (z 20000) keeps its order relative to the save pill and Auto Graph (also 20000). Only the debug HUD's relative order changes, which is debug-only.
- NodeCanvas.jsx: 16,464 → **16,430**.

## Verification
- New F25: the tuner opens from the store, and Randomize moves nodes. It fails when `layoutNodes` returns nothing. F19b's View → Auto Layout still passes.
- `test:ci` PASS. Playwright: 52 passed (F12 expected). `lint:undef` PASS.
