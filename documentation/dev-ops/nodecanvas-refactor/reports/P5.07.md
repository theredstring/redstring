# P5.07 report: context-menu builders

**Status:** done as a verbatim move (orchestrator). Branch `refactor/p5.07`.

## What changed
- `src/components/canvas/menus/contextMenus.jsx`: `buildCanvasContextMenuOptions(clientX, clientY, ctx)` and `buildNodeContextMenuOptions(instanceId, ctx)`. These are the bodies of NodeCanvas's `getCanvasContextMenuOptions` and `getContextMenuOptions`, moved byte for byte.
- NodeCanvas keeps both `useCallback`s with the same dependency lists, passing the values they closed over as `ctx`.
- The move used the P5.01a method:
  - free variables found by ESLint in isolation, now counting `react/jsx-no-undef` too, because these bodies hold JSX icons;
  - NodeCanvas's imports copied across with rewritten paths;
  - a check for any `ctx` name declared after the callback (there are none; `startHurtleAnimation` now comes earlier).
- **39 NodeCanvas imports pruned** that this move and P5.01a left unused. P5.01a should have pruned its own share; caught here.

NodeCanvas.jsx: 14,054 → **13,742**.

## Still open
The card's "plus commands" half: actions calling registered canvas commands and reading `getState()` at fire time. As with P5.01b, that changes behaviour action by action, so it's separate.

## Verification
- F12 asserts both menus' exact item lists (no item dropped, added or reordered), that an item runs, and that clicking off closes. It passes.
- `test:ci` PASS. `lint:undef` PASS. Playwright 56 passed. `npm run build` passes.
