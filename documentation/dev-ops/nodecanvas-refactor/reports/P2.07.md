# P2.07 report: `DeletionGhostLayer`

**Status:** done (orchestrator).

## What changed
- **canvasUIStore:** `deletionGhosts` plus `addDeletionGhosts(ghosts)` and `removeDeletionGhost(id)`. Adding nothing, or removing an unknown id, writes nothing.
- **`src/components/canvas/layers/DeletionGhostLayer.jsx`** (memo, no props) renders the ghost `<rect>`s where NodeCanvas did (inside the content group, above the group titles). Each removes itself on `animationend`.
- **NodeCanvas:** `captureDeletionGhosts` builds the ghosts as before (shuffle, stagger) and adds them to the store. The `deletionAnimations` state is gone.
- **Accept met by construction:** NodeCanvas no longer holds ghost state, so ghost cleanup causes 0 NodeCanvas commits. Before, each ghost's `animationend` was a NodeCanvas render.
- NodeCanvas.jsx: 16,430 → **16,415**.

## Verification
- New store tests (add/remove, no-op writes).
- New F26: deleting two nodes shows two ghosts, which then clear from the DOM and the store. F13's Delete still passes.
- `test:ci` PASS. Playwright: 53 passed (F12 expected). `lint:undef` PASS.
