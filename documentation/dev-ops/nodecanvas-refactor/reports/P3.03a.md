# P3.03a report: group layout as data

**Status:** part a done (orchestrator). Branch `refactor/p3.03`.

## What changed
- **`components/canvas/groups/groupLayouts.js` `computeGroupLayouts(...)`** holds the groups pass's computation, moved verbatim:
  - the depth sort and effective names (including the rename draft);
  - the shared layout context and `computeGroupLayout` per group;
  - title rects, anchor positions, and the thing-group member and anchor sets.
- NodeCanvas memoizes it on those inputs plus `labelFontVersion`. The pass used to re-measure label widths on every render; the memo re-measures once the web font arrives.
- The JSX pass keeps building elements, and publishes `titleRects`/`anchorPositions` to the same refs at the same point in render, so their readers see exactly what they saw.
- `groupStructure`'s own maps replace the refs mirrored from it, which were assigned in the same render.
- NodeCanvas.jsx: 12,994 → **12,884**.

## Not in this part (P3.03b / P3.04)
- Feeding anchor geometry into `cleanLaneOffsets`, `lombardiTangents` and `labelCrossingIndex` as a real dependency, which would remove their one-render lag. That changes transient edge and label placement, so it belongs with the label work Grant reviews (P3.05).
- Keying the anchor flush on the layout output instead of every commit.
- The memoized group layers (P3.04).

## Verification
- **Markup equivalence:** the whole `svg.canvas` outerHTML was captured on the wave5 tip (baseline worktree) and on this branch.
  - **Byte-identical:** small (default, grid on, and mid-rename of a group title), stress (46 group labels), and chambers zoomed out (13 groups).
  - **Chambers framed** differs only in edge-label sprite `<image>`s and one edge-label rect. Two baseline runs differ from each other the same way (async sprite baking), and the second baseline run matches this branch exactly outside the sprites.
  - Chambers markup stayed in the session scratchpad; only counts are reported here.
- `test:ci` PASS. `lint:undef` PASS: it caught a leftover `groupLabelScale` before the first build. Playwright 58 passed, F11 (groups) included.
- **Perf** (chambers): S5, S7 and S8 render counts unchanged, timings within noise. Group layout wasn't a large per-render cost on this fixture; the value is structural.
